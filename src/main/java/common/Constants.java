package common;

import utils.Property;

import java.util.*;

import static common.Extensions.*;

public class Constants {
    public static Property USERNAME;
    public static Property PASSWORD;
    public static Property ANDROID_ID;
    public static Property ANDROID_ID2;
    public static Property MUSIC_FOLDER;
    public static Property DEBUG;

    public static Properties properties;

    public static List<Extensions> validExtensions = Arrays.asList(MP3, OGG, FLAC, MP4, AIFF, WAV, WMA, DSF, M4A);
}